@namespace("com.raha.pravin")
protocol Greeter {

  record HelloRequest {
    string name;
  }

  record HelloResponse {
    string greeting;
    boolean happy;
  }

  HelloResponse SayHello(HelloRequest request);

}
