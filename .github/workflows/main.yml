name: CI
on:
  push:
    branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: redkubes/docusaurus-deploy-action@v1.2
        with:
          git-user: pravin-raha
          git-password: ${{ secrets.GITHUB_TOKEN }}
